info:
  name: esperado una lista array de municipios
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:4000/api/municipalities?departmentId=29
  params:
    - name: departmentId
      value: "29"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
