info:
  name: " esperado 200 con ok: true"
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:4000/health
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
