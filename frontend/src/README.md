# Comentarios de la carpeta src

Esta carpeta contiene el punto de entrada del frontend y los estilos globales.

- `main.tsx`: arranque de React.
- `App.tsx`: orquestación principal del formulario.
- `index.css` y `App.css`: estilos globales y de impresión.
- `modules/`: módulos funcionales del formulario EPS.